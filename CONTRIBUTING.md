# Contributing to PayTrace

Preserve the deterministic ownership boundary: financial state, idempotency, invariants, and recovery actions belong to Java code. Add tests for state transitions, persisted evidence, and behavior that changes a recovery result. Synthetic data is mandatory.
