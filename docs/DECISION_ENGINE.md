# Decision Engine

Decision intelligence is isolated behind `DecisionIntelligencePort`. The local adapter derives bounded duplicate risk and retry safety from deterministic evidence. Recovery policy is a separate Java service and selects allowed recommendation values without allowing any model to execute money movement.
