# Demo Scenarios

The simulator exposes happy payment, provider timeout, duplicate retry, duplicate webhook, out-of-order webhook, missing webhook, ledger/provider mismatch, duplicate capture, refund timeout, over-refund, missing settlement, ledger imbalance, and safe retry labels. Every run uses synthetic references and returns an investigation payload.
