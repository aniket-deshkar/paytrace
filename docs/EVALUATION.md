# Evaluation

Evaluation fixtures use controlled synthetic scenarios. Expected results state the canonical state, incident category, invariant outcomes, and recovery action. No aggregate quality numbers are asserted because the repository does not train or benchmark a probabilistic classifier.
